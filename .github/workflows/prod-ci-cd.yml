name: Production CI/CD (Render)

on:
  push:
    branches: [ prod ]
  pull_request:
    branches: [ prod ]

env:
  PYTHON_VERSION: '3.13'
  DJANGO_SETTINGS_MODULE: website.settings

jobs:

  lint:
    name: Code Quality Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'
      - run: |
          pip install -r requirements.txt
          pip install flake8 black pylint-django
      - run: flake8 --exclude=migrations,__pycache__,venv --max-line-length=120 --extend-ignore=E203,W503 || true
      - run: black --check --exclude='migrations|__pycache__|venv' . || true
      - run: |
          pylint --load-plugins=pylint_django \
                 --django-settings-module=website.settings \
                 --disable=C0111,C0103,R0903,W0212,E1101 \
                 --ignore=migrations,__pycache__ \
                 main/ website/ || true

  test:
    name: Run Tests
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: test_db
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_password
        ports:
          - 5432:5432
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'
      - name: Install deps
        run: |
          pip install -r requirements.txt
          pip install pytest pytest-django pytest-cov
      - name: Create .env for Django tests
        run: |
          echo "SECRET_KEY=test_secret" > .env
          echo "DEBUG=True" >> .env
          echo "ALLOWED_HOSTS=localhost,127.0.0.1" >> .env
          echo "CLOUDINARY_URL=cloudinary://test_key:test_secret@test_cloud" >> .env
          echo "DB_NAME=test_db" >> .env
          echo "DB_USER=test_user" >> .env
          echo "DB_PASSWORD=test_password" >> .env
          echo "DB_HOST=localhost" >> .env
          echo "DB_PORT=5432" >> .env
      - run: python manage.py migrate --noinput
      - run: pytest --cov=. --cov-report=term-missing
      - uses: codecov/codecov-action@v4
        continue-on-error: true

  security:
    name: Security Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      - run: pip install safety bandit -r requirements.txt
      - run: safety check --json --output safety-report.json || true
      - run: bandit -r main/ website/ -f json -o bandit-report.json || true
      - uses: actions/upload-artifact@v4
        with:
          name: security-reports-prod
          path: |
            safety-report.json
            bandit-report.json

  docker-check:
    name: Validate Docker
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker build -t test . || true
      - run: docker-compose config || true
      - run: bash -n entrypoint.sh || true

  deploy-render:
    name: Deploy to Render
    runs-on: ubuntu-latest
    needs: [test, security, docker-check]
    if: github.ref == 'refs/heads/prod'
    environment:
      name: production
      url: https://curvative-parameter.onrender.com
    steps:
      - uses: actions/checkout@v4

      - name: Verify Render Hook Secret
        run: |
          if [ -z "${{ secrets.RENDER_DEPLOY_HOOK_URL }}" ]; then
            echo "❌ Missing RENDER_DEPLOY_HOOK_URL secret for environment 'production'"
            exit 1
          else
            echo "✅ Render deploy hook found in environment 'production'"
          fi

      - name: Create .env for production
        run: |
          echo "SECRET_KEY=${{ secrets.SECRET_KEY }}" > .env
          echo "DEBUG=False" >> .env
          echo "ALLOWED_HOSTS=curvative-parameter.onrender.com" >> .env
          echo "CLOUDINARY_URL=${{ secrets.CLOUDINARY_URL }}" >> .env
          echo "DB_NAME=${{ secrets.DB_NAME }}" >> .env
          echo "DB_USER=${{ secrets.DB_USER }}" >> .env
          echo "DB_PASSWORD=${{ secrets.DB_PASSWORD }}" >> .env
          echo "DB_HOST=${{ secrets.DB_HOST }}" >> .env
          echo "DB_PORT=${{ secrets.DB_PORT }}" >> .env
          echo "✅ .env created for production"

      - name: Deploy to Render
        env:
          RENDER_HOOK: ${{ secrets.RENDER_DEPLOY_HOOK_URL }}
        run: |
          echo "🚀 Triggering Render deploy..."
          if [ -z "$RENDER_HOOK" ]; then
            echo "❌ RENDER_DEPLOY_HOOK_URL is empty or not accessible"
            exit 1
          fi
          echo "✅ Render hook found. Deploying..."
          curl -fsSL "$RENDER_HOOK" || {
            echo "❌ Render deploy failed"
            exit 1
          }

  release:
    name: Tag Release
    runs-on: ubuntu-latest
    needs: deploy-render
    if: github.ref == 'refs/heads/prod'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Create tag
        run: |
          VERSION="v$(date +'%Y.%m.%d')-prod.${{ github.run_number }}"
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git tag -a "$VERSION" -m "Production release"
          git push origin "$VERSION"
